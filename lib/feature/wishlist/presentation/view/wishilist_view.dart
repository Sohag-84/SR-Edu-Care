import 'package:flutter/material.dart';

class WishilistView extends StatelessWidget {
  const WishilistView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Wishlist")));
  }
}
